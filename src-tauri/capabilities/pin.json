{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "pin",
  "description": "Minimum capabilities for the pinned screenshot window",
  "windows": ["pin"],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging"
  ]
}
